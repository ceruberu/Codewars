function listToArray(list) {
  var answer = [];
 
  while ( true ) {
  answer.push(list.value); // push the value to the answer array
  if ( list.next === null ) {
  break } // if the value of the key "next" is null, break
  list = list.next; // if it wasn't broken, get into the next object;
  };
  
  return answer;
}// function
